{
  "name": "desafio-nodis",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "jest": {
    "modulePathIgnorePatterns": [
      "<rootDir>/__tests__/utils/"
    ]
  },
  "scripts": {
    "dev": "nodemon server.js",
    "initdb": "cd db && knex migrate:latest && knex seed:run",
    "prestart": "npm run initdb",
    "start": "node server.js",
    "pretest": "npm run initdb",
    "test": "set NODE_ENV=testing && jest || true",
    "posttest": "cd db && knex migrate:rollback",
    "coverage": "npm run test -- --coverage"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "faker": "^5.5.1",
    "jest": "^26.6.3",
    "nodemon": "^2.0.7",
    "supertest": "^6.1.3"
  },
  "dependencies": {
    "express": "^4.17.1",
    "express-async-handler": "^1.1.4",
    "express-validator": "^6.10.0",
    "knex": "^0.95.2",
    "moment": "^2.29.1",
    "mysql": "^2.18.1",
    "swagger-ui-express": "^4.1.6"
  }
}
